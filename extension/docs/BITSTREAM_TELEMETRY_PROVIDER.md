# Bitstream Telemetry Provider

**Moved to the portable developer kit:**

**[`bitstream-telemetry-provider/`](./bitstream-telemetry-provider/README.md)**

Upload the **`bitstream-telemetry-provider/`** folder to external developers or AI coding agents. It contains README, **SKILL** (AI workflow: connect live first, mock fallback, plain Bitstream/Simulator language), event docs, JSON Schema, and `sensor-catalog.v1.json` in one place.
