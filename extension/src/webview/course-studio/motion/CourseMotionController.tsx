import type { ReactNode } from "react";

/**
 * Phase 7i stub — data tweens live in `useCourseDisplayTween`.
 * Authored enter/emphasis timelines will mount here in a later phase.
 */
export function CourseMotionController({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
