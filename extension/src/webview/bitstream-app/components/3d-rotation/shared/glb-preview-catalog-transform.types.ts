/** Optional host override from catalog `animationLab.transform` metadata. */
export type GlbPreviewCatalogTransform = {
  position?: [number, number, number];
  rotationDeg?: [number, number, number];
  scale?: [number, number, number];
};
