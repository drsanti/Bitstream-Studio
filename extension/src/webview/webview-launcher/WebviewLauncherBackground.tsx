import { WebviewFlowShellBackground } from "./WebviewFlowShellBackground";

/**
 * Launcher background: deep space nebula, flow particles, edge vignette.
 */
export function WebviewLauncherBackground() {
  return <WebviewFlowShellBackground interactionRootClass="webview-launcher" />;
}
