/** Shared layout tokens for flow graph nodes (chrome only). */
export const FLOW_NODE_MIN_WIDTH_CLASS = "min-w-[170px]";

/** Header trailing chips (LIVE / family tag / Invalid) — Inter, compact pill. */
export const FLOW_NODE_HEADER_BADGE_CLASS =
  "rounded-md border px-1.5 py-0.5 text-[9px] font-semibold uppercase tracking-wide leading-none";
