/** TRNSelect on flow node cards — never use `min-w-*` that exceeds the shell width. */
export const FLOW_NODE_TRN_SELECT_CLASS = "w-full min-w-0 max-w-full";

export const FLOW_NODE_TRN_SELECT_BUTTON_SM_CLASS = "h-8 w-full min-w-0 max-w-full text-[12px]";
