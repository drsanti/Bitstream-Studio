# Presentation v1 — frozen

**Status:** Maintenance-only backup (legacy slide deck).

The shipped **Presentation** workspace (`extension/src/webview/presentation/`) remains available as **v1**. New alive-document authoring features belong in **Course Studio** (`extension/src/webview/course-studio/`).

## Policy

- **Allowed:** bug fixes, broken slide fixes, theory reader typos, critical parity with live store
- **Not allowed:** new slide layouts, grid composer, diagram editor, maintainer tools, large content migrations

## Successor

**Course Studio** — `?workspace=course-studio` (dev) — see `course-studio/docs/ARCHITECTURE.md` and `presentation/docs/DEVELOPMENT_PLAN.md` §17.
