**Duration:** ~5 min

**Talking points**
- sensorId 0–3 maps to palette nodes — same ids as `bss-demo-telemetry`.
- Tap nodes reduce clutter when only one output is needed.
- Deep port lists live in each sensor chapter's `*-studio-node` slide.

**Demo script**
- Open Sensor Studio → Flow → add BMI270 + a Humidity tap from SHT40 → wire to a gauge.

**Q&A prompts**
- When use full node vs tap?
- Do tap nodes cost extra decode work?
