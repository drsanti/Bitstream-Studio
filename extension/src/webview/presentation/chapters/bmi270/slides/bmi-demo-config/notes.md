**Duration:** ~8 min lab — range vs resolution trade-off.

Explain that wider ±g range lowers LSB resolution. ODR affects filter bandwidth and host sample rate.

**Important:** Presentation does not write SENSOR_CFG on the wire in v0.1 — students apply the same numbers in **Sensor Telemetry** (local draft). Wire restore (`SENSOR_CFG_SET`) is platform backlog.
