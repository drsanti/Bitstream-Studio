/**
 * @deprecated Import from `../device/firmware-simulator` instead.
 * Re-exported for backward compatibility.
 */
export {
  BsFirmwareSimulator,
  BsMockFirmware,
  createBsMockLoopback,
  type BsFirmwareSimStatePayload,
  type BsFirmwareSimulatorOptions,
  type BsMockFirmwareOptions,
} from "../device/firmware-simulator";
